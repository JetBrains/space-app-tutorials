ktor {
    deployment {
        port = 3000
        port = ${?PORT}
        callGroupSize = 8
    }
    application {
        modules = [org.homepage.ApplicationKt.module]
    }
}

postgresql {
    host = localhost
    port = 5432
    user = root
    password = spaceEvents
    database = spaceEvents
}
