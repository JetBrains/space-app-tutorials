space {
    clientId = ""

    # use environment variable in production to pass the secret
    clientSecret = ""
    clientSecret = ${?CHAT_BOT_CLIENT_SECRET}

    serverUrl = "https://my-company.jetbrains.space/"
}
